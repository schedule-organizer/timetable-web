# REST API base URL — when calling a real backend.
# Leave empty to use MSW mocks (default for local dev).
VITE_API_BASE_URL=

# WebSocket endpoint — only required when live backend integration is enabled.
# VITE_WS_URL=ws://localhost:8080/ws
